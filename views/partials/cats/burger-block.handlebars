<li>
	{{burger_name}}

	<button class="change-scarf" data-id="{{id}}" data-newsleep="{{scarf}}">
		{{#if scarf}}Scarf TIME!{{else}}WAKE UP!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
